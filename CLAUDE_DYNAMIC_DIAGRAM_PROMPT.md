# Claude Code Prompt: Dynamic LangGraph Orchestrator Diagram

## Objective
Transform the static hardcoded LangGraph Orchestrator Structure diagram in `/home/appadmin/projects/Ram_Projects/agents-db/views/solution-detail.ejs` into a dynamic, interactive diagram that:
- Generates from the current workflow pipeline state (solution.steps array)
- Auto-regenerates when agents are added/removed/reordered
- Shows supervisor and gate nodes between agents automatically
- Displays real-time connections and data flow
- Responds to drag-and-drop reordering
- Shows agent status (READY/STOPPED) with appropriate colors
- Handles dynamic agent additions from the Available Agents panel

## Context
- **Project Path**: `/home/appadmin/projects/Ram_Projects/agents-db`
- **Main File**: `/home/appadmin/projects/Ram_Projects/agents-db/views/solution-detail.ejs`
- **Backend**: LangGraph orchestration with supervisor nodes and gates
- **Current Status**: Hardcoded diagram on lines 104-140, needs to be replaced with dynamic rendering
- **Data Source**: `solution.steps` array containing agent definitions with id, name, endpointName, order, inputMapping, description

## Task Description

### 1. Analyze Current State
First, examine the static diagram structure (lines 104-140 in views/solution-detail.ejs) to understand:
- Node types: START, Agent (blue #3B82F6), Supervisor (purple #7C3AED), Gates (orange #F59E0B), END
- Current hardcoded agents: Profile, Quality, Classify, AutoLoad
- Flow pattern: Agent → Supervisor → Gate → Agent → Supervisor → Gate ...
- Node styling and layout

### 2. Create JavaScript Function: `generateDynamicDiagram()`

Create a comprehensive JavaScript function with the following specifications:

```javascript
function generateDynamicDiagram(solutionSteps) {
    /*
    Input: solutionSteps - Array of step objects with properties:
      - id: unique identifier
      - name: display name (e.g., "Data Quality Check")
      - endpointName: Databricks endpoint (e.g., "mit_data_quality_agent_endpoint")
      - order: numeric order in pipeline
      - status: optional READY/STOPPED indicator

    Output: Returns HTML string for complete diagram

    Logic:
    1. Build nodes array in correct order:
       - START node (green)
       - For each step in sorted order:
         - Agent node (blue, with name and endpoint)
         - Supervisor node (purple, generic "analyze")
         - Gate node (orange, with human/auto decision indicator)
       - END node (green)

    2. Generate HTML using flex layout:
       - Container: flex row, center-aligned, wrapping
       - Connectors: gray arrows (→) between nodes
       - Nodes: divs with unique styling by type

    3. Node colors:
       - Blue (#3B82F6): Agent nodes with emoji
       - Purple (#7C3AED): Supervisor nodes
       - Orange (#F59E0B): Decision gates
       - Green (#10B981): START/END
       - Gray dashed border: Gates

    4. Status handling:
       - If step.status === "STOPPED", add opacity: 0.5, strikethrough
       - If step.status === "READY", add green badge
       - Default: normal appearance

    5. Return complete HTML with legend at bottom
    */
}
```

### 3. Event Listeners & Integration Points

Add event listeners for:

1. **Agent Addition** (from Available Agents panel):
   - Listen for clicks on "+ Add" buttons
   - After agent is added to workflow, call `generateDynamicDiagram()`
   - Update #langgraphDiagram with new HTML

2. **Agent Removal**:
   - Listen for clicks on delete (×) buttons
   - After agent is removed, call `generateDynamicDiagram()`

3. **Drag & Drop Reordering**:
   - Listen for dragend events on `.workflow-agent` cards
   - After reordering completes, call `generateDynamicDiagram()`
   - Ensure steps array is sorted by order property

4. **Step Edit Completion**:
   - Listen for modal close/save events
   - If step properties changed, call `generateDynamicDiagram()`

### 4. HTML Container & Styling

Replace the static diagram section (lines 104-140) with:

```html
<!-- LangGraph Orchestrator Visualization (Top Layer) -->
<div class="clean-section" style="margin-top: 15px;">
    <h3>LangGraph Orchestrator Structure</h3>
    <p style="font-size: 0.85rem; color: var(--gray-600); margin-bottom: 12px;">
        Autonomous supervisor-guided execution flow with intelligent decision-making
    </p>
    <div style="padding:20px;background:#f8fafc;border-radius:12px;overflow-x:auto;">
        <div id="langgraphDiagram" style="display:flex;align-items:center;gap:6px;flex-wrap:wrap;justify-content:center;">
            <!-- Dynamically generated by JavaScript -->
        </div>
    </div>
    <div style="display:flex;gap:16px;justify-content:center;margin-top:12px;font-size:10px;color:#6B7280;flex-wrap:wrap;">
        <span>🔵 Agent</span><span>🟣 Supervisor</span><span>🟡 Decision Gate</span><span>🟢 Start/End</span>
    </div>
</div>
```

### 5. Supervisor Node Logic

Between each agent, automatically insert a supervisor node with:
- Background: Purple (#7C3AED)
- Label: "🧠 Supervisor"
- Subtitle: Context-based (e.g., "analyze", "score check", "PII check", "code review")
- Size: Small (4px 10px padding)

Supervisor subtitles should be determined by:
- After "Profile" agent → "structure analyze"
- After "Quality" agent → "score check"
- After "Classify" agent → "PII check"
- After "AutoLoad" agent → "code review"
- After custom agents → "analyze"

### 6. Gate Node Logic

After each supervisor, insert a decision gate node with:
- Background: Orange (#F59E0B)
- Border: 2px dashed #D97706
- Label: Gate type depends on agent:
  - After Quality → "Quality Gate: ≥80% auto | <60% halt"
  - After Classify → "PII Gate: human confirm"
  - After AutoLoad → "Review: approve code"
  - Custom agents → "Decision Gate"
- Size: Medium (6px 12px padding)

### 7. Connector Arrows

Between each node pair, add:
- Visual: Gray arrow → (color: #9CA3AF)
- Hover effect (optional): Slightly darker on hover
- Responsive: Wrap naturally on smaller screens

### 8. Implementation Requirements

**Use only vanilla JavaScript**, no external libraries:
- No jQuery, no D3, no Mermaid
- Pure HTML/CSS/JavaScript
- CSS Grid or Flexbox for layout
- Browser-native APIs

**Maintainability**:
- Add comprehensive comments
- Separate functions for node generation, styling, connector rendering
- Make it easy to extend with new supervisor types or gates
- Avoid hardcoded values for colors/sizes

**Performance**:
- Call `generateDynamicDiagram()` only when necessary
- Debounce rapid updates if multiple changes occur
- Render time should be <100ms for typical workflows (5-10 agents)

**Accessibility**:
- Add title attributes to nodes for tooltips
- Use semantic class names
- Ensure sufficient color contrast

### 9. Integration Checklist

After implementation:
- [ ] Remove hardcoded diagram HTML (lines 104-140)
- [ ] Add `generateDynamicDiagram()` function to `<script>` section
- [ ] Add event listener hooks in existing agent add/remove/reorder handlers
- [ ] Test with empty workflow (shows START → END only)
- [ ] Test with single agent (shows START → Agent → Supervisor → Gate → END)
- [ ] Test with multiple agents (shows full chain with all supervisors/gates)
- [ ] Test drag-and-drop reordering (diagram updates immediately)
- [ ] Test adding new agent (diagram extends automatically)
- [ ] Test removing agent (diagram contracts automatically)
- [ ] Verify responsive layout on mobile (horizontal scroll if needed)
- [ ] Verify diagram updates in real-time during execution polling

### 10. Testing Scenarios

1. **Empty Workflow**: Click "+ Add Agent Step", observe diagram stays simple
2. **Single Agent**: Add "Data Quality Check", verify: START → Agent → Supervisor → Gate → END
3. **Multi-Agent Pipeline**: Add 3+ agents, verify each gets supervisor + gate
4. **Agent Reordering**: Drag Quality above Classifier, verify diagram reflects new order immediately
5. **Agent Removal**: Delete middle agent, verify chain reconnects correctly
6. **Rapid Updates**: Add, remove, reorder in quick succession, verify no visual glitches
7. **Execution Flow**: Start workflow, verify diagram remains visible and clean during execution

### 11. Code Style Guidelines

- Use camelCase for JavaScript variables/functions
- Use descriptive names: `generateDiagramNode()`, not `gen()`
- Comments for complex logic sections
- Group related code with blank lines
- Follow existing codebase style from solution-detail.ejs
- Use consistent indentation (2 spaces, matching EJS file)

### 12. Success Criteria

The dynamic diagram is complete when:
1. All static HTML is replaced with JavaScript-generated content
2. Adding agents to workflow automatically extends the diagram
3. Removing agents automatically contracts the diagram
4. Drag-and-drop reordering updates diagram in real-time
5. Supervisor nodes appear automatically between agents
6. Decision gates appear automatically after supervisors
7. All agents show correct endpoint names and descriptions
8. Diagram is responsive and scrollable on small screens
9. No console errors or warnings
10. Execution continues to display diagram during workflow run

## Output Format

Provide a complete, production-ready JavaScript implementation that:
- Can be pasted directly into the `<script>` section of views/solution-detail.ejs
- Includes all necessary functions and event listeners
- Has clear comments for maintainability
- Includes error handling for edge cases
- Works with the existing workflow management system

## Files to Modify

Primary:
- `/home/appadmin/projects/Ram_Projects/agents-db/views/solution-detail.ejs` - Lines 104-140 (replace static diagram), add JavaScript function, add event listeners

Optional (document usage):
- `/home/appadmin/projects/Ram_Projects/agents-db/CLAUDE_DYNAMIC_DIAGRAM_PROMPT.md` - This file (or update existing docs)

## Start Implementation

1. Read the current views/solution-detail.ejs file (full content)
2. Identify all existing JavaScript functions related to agent management
3. Create the `generateDynamicDiagram()` function
4. Create helper functions for node generation
5. Add event listeners to existing agent add/remove/reorder handlers
6. Replace the static HTML section with dynamic container
7. Test with the workflow designer to ensure integration
8. Verify all event flows trigger diagram regeneration
9. Commit changes with message: "feat: make LangGraph diagram dynamic from workflow state"

## Additional Notes

- The diagram should reflect only what's in `solution.steps`, not the hardcoded agents
- If solution.steps is empty, show a simple START → END
- The diagram should work alongside existing execution log and state inspector
- No breaking changes to existing functionality
- Maintain visual consistency with current design system (colors, fonts, spacing)
- Consider future extensibility for custom agent types or exotic node configurations
